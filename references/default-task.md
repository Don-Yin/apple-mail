list all inbox emails with `list-recent --include-content`, then for each email call `read-email --message-id MID` to get the full content. after reading every email, present a summary table with columns: sender, subject, date, and proposed action (reply, archive, delete, follow up, or flag). at the end, propose a separate list of emails that are definitely safe to delete (e.g., expired transactional notifications, merged PR notifications, old CI failures, marketing newsletters, past event reminders). ask the user to confirm before executing any action.
